package grondag.adversity.superblock.model.painter;

import java.util.Collection;
import java.util.List;

import grondag.adversity.library.model.quadfactory.RawQuad;
import grondag.adversity.superblock.model.state.ModelStateFactory.ModelState;

public class SurfaceQuadPainters
{

    public static void paintSurfaceTiles(ModelState modelState, int painterIndex, Collection<RawQuad> shapeQuads, List<RawQuad> outputQuads)
    {
        
    }
    
    public static void paintSurfaceCylinder(ModelState modelState, int painterIndex, Collection<RawQuad> shapeQuads, List<RawQuad> outputQuads)
    {
        
    }
    
    public static void paintSurfaceTorus(ModelState modelState, int painterIndex, Collection<RawQuad> shapeQuads, List<RawQuad> outputQuads)
    {
        
    }
}
