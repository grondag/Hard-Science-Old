package grondag.adversity.superblock.model.painter;

import java.util.Collection;
import java.util.List;

import grondag.adversity.library.model.quadfactory.RawQuad;
import grondag.adversity.superblock.model.state.ModelStateFactory.ModelState;

public class CubicQuadPainters
{

    public static void paintCubicTiles(ModelState modelState, int painterIndex, Collection<RawQuad> shapeQuads, List<RawQuad> outputQuads)
    {
        
    }
    
    public static void paintCubicBigTex(ModelState modelState, int painterIndex, Collection<RawQuad> shapeQuads, List<RawQuad> outputQuads)
    {
        
    }
    
    public static void paintCubicBorder(ModelState modelState, int painterIndex, Collection<RawQuad> shapeQuads, List<RawQuad> outputQuads)
    {
        
    }
    
    public static void paintCubicMasonry(ModelState modelState, int painterIndex, Collection<RawQuad> shapeQuads, List<RawQuad> outputQuads)
    {
        
    }
}
